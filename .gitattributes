# Auto detect text files and perform LF normalization
* text=auto

# Scripts Bash deben usar LF (Linux)
*.sh text eol=lf

# Scripts PowerShell usan CRLF (Windows)
*.ps1 text eol=crlf

# SQL files
*.sql text eol=lf

