# ============================================
# VARIABLES DE ENTORNO - PRODUCCIÓN
# ============================================
# 
# INSTRUCCIONES:
# 1. Copia este archivo: cp env.prod.example .env
# 2. Rellena TODAS las variables con valores reales
# 3. NUNCA commitees .env al repositorio
#
# ============================================

# --------------------------------------------
# BASE DE DATOS
# --------------------------------------------
POSTGRES_DB=gmarm_prod
POSTGRES_USER=postgres
POSTGRES_PASSWORD=              # ✅ OBLIGATORIO - Genera un password seguro

# --------------------------------------------
# BACKEND / API
# --------------------------------------------
JWT_SECRET=                     # ✅ OBLIGATORIO - Genera un secreto de 64+ caracteres
API_URL=https://api.gmarm.com   # URL del backend en producción

# --------------------------------------------
# FRONTEND
# --------------------------------------------
VITE_API_BASE_URL=https://api.gmarm.com

# --------------------------------------------
# CORS (Backend)
# --------------------------------------------
CORS_ORIGINS=https://gmarm.com,https://www.gmarm.com

# --------------------------------------------
# EMAILS (Opcional - para notificaciones)
# --------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=noreply@gmarm.com

# --------------------------------------------
# MONITOREO (Opcional)
# --------------------------------------------
SENTRY_DSN=                     # Para tracking de errores
SENTRY_ENVIRONMENT=production

# ============================================
# NOTAS DE SEGURIDAD
# ============================================
#
# 1. POSTGRES_PASSWORD:
#    - Mínimo 20 caracteres
#    - Mezcla de mayúsculas, minúsculas, números y símbolos
#    - Genera con: openssl rand -base64 32
#
# 2. JWT_SECRET:
#    - Mínimo 64 caracteres
#    - Genera con: openssl rand -base64 64
#
# 3. NUNCA uses valores por defecto en producción
#
# 4. Asegúrate de que .env está en .gitignore
#
# ============================================
